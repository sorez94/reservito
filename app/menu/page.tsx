import React from 'react';
import MenuCard from "@/app/menu/menu-component/MenuCard";

const Page = () => {
    return (
        <div>
            <MenuCard/>
        </div>
    );
};

export default Page;